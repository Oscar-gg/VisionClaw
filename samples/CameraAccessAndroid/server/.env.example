# Required: Secret token you make up — must match CLAUDE_CODE_TOKEN in the Android app's Secrets.kt
CLAUDE_CODE_TOKEN=your-secret-token-here

# Optional: SYNAPSE API base URL (default: http://localhost:8000)
# SYNAPSE_API_URL=http://localhost:8000

# Optional: Override default port (default: 18789)
# PORT=18789
